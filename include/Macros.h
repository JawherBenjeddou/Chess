#pragma once
// Screen Size
#define SCREEN_WIDTH 640
#define SCREEN_HEIGHT 640
// Piece Size
#define PIECE_SIZE 75

#define SUPER Piece

// Minimum and maximum macros
#define __MAX(a,b) (((a) > (b)) ? (a) : (b))
#define __MIN(a,b) (((a) < (b)) ? (a) : (b))
